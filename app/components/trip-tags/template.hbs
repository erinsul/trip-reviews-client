<h4>{{trip.place.name}}, {{trip.place.country}}</h4>
<p>My tags: to be added when I fix back end</p>
<p>Add more tags:</p>
{{#each tags as |tag|}}
  {{tag-button tag=tag click='addDescription'}}
{{/each}}
